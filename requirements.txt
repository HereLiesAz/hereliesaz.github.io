numpy
opencv-python-headless
Pillow
pillow-heif

# Off-device ML — theater_baker.py uses gradio_client to call the
# Depth-Anything-V2 HF Space. No torch/transformers required locally.
gradio_client
huggingface_hub
