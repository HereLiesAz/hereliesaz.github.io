RewriteEngine On

# If the request is for an existing file or directory, serve it directly.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Otherwise route to index.html (SPA fallback).
RewriteRule ^ index.html [L]
